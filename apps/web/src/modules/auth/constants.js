// Auth module constants — the sign-in route. Nothing else in the app
// should spell "/login" itself; app/router.jsx, the app/guards, and LoginPage all
// import this one value.
export const LOGIN_ROUTE_PATH = "/login";
